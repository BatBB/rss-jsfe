import { IState } from '../interfaces/interfaces';

const state: IState = {
  garagePage: 1,
  winnersPage: 1,
  cars: [],
  carStatus: new Map(),
  engineStatus: new Map(),
};

export default state;
